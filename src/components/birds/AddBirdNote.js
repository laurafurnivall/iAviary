import { useState } from "react"
import { useParams } from "react-router-dom"

export const AddBirdNote = () => {

    // { BirdId } = useParams()
    // [newNote, addNewNote] = useState({

    // })
    // return <>
    
    // </>
}